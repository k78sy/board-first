<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.green.board.application.BoardMapper">
    <insert id="save">
        INSERT INTO board
        SET
        title = #{title},
        contents = #{contents};
    </insert>
    <select id="allList">
        SELECT id, title, created_at AS createdAt
        FROM board
        ORDER BY createdAt DESC;
    </select>
    <select id="detailOne">
        SELECT id, title, contents, created_at AS createdAt
        FROM board
        WHERE id = #{id};
    </select>
    <delete id="delete">
        DELETE
        FROM board
        WHERE id = #{id};
    </delete>
    <update id="update">
        UPDATE board
        SET
        title = #{title},
        contents = #{contents}
        WHERE id = #{id};
    </update>
</mapper>